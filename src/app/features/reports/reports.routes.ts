import type { Routes } from '@angular/router';
import { ReportPreviewComponent } from './report-preview.component';

export const reportsRoutes: Routes = [{ path: '', component: ReportPreviewComponent }];
