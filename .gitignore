# Working notes — never commit
.scratch/

# .NET secrets — never commit
appsettings.Production.json
appsettings.*.local.json
**/secrets.json
**/.env
**/.env.local

# OS / editor
.DS_Store
.idea/
*.swp

# Build outputs (per-project .gitignore covers more)
bin/
obj/
node_modules/

# Legacy reference source — local only, not committed
reference/old/